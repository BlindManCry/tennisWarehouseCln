export function wrap_classnameWidth(index) {
  return index === 0
    ? "w-[210px]"
    : index === 1
    ? "w-[120px]"
    : index === 2
    ? "w-[210px]"
    : index === 3
    ? "w-[120px]"
    : index === 4
    ? "w-[210px]"
    : index === 5
    ? "w-[210px]"
    : index === 6
    ? "w-[210px]"
    : index === 7
    ? "w-[210px]"
    : index === 8
    ? "w-[210px]"
    : index === 9
    ? "w-[210px]"
    : index === 10
    ? "w-[210px]"
    : index === 11
    ? "w-[210px]"
    : index === 12
    ? "w-[210px]"
    : index === 13
    ? "w-[210px]"
    : index === 14
    ? "w-[210px]"
    : "";
}

export function wrap_classnameHeight(index) {
  return index === 0
    ? "h-[157px]"
    : index === 1
    ? "h-[278px]"
    : index === 2
    ? "h-[157px]"
    : index === 3
    ? "h-[278px]"
    : index === 4
    ? "h-[157px]"
    : index === 5
    ? "h-[280px]"
    : index === 6
    ? "h-[280px]"
    : index === 7
    ? "h-[157px]"
    : index === 8
    ? "h-[280px]"
    : index === 9
    ? "h-[280px]"
    : index === 10
    ? "h-[280px]"
    : index === 11
    ? "h-[280px]"
    : index === 12
    ? "h-[280px]"
    : index === 13
    ? "h-[280px]"
    : index === 14
    ? "h-[280px]"
    : "";
}

export function wrap_translateClassnameInBabolat(translate) {
  return `translate-x-[${translate}%]`;
}

// export function wrap_translateClassnameInBabolat(translate) {
//   return translate === 0
//     ? "translate-x-0"
//     : translate === 100
//     ? "translate-x-[100%]"
//     : translate === 200
//     ? "translate-x-[200%]"
//     : translate === 300
//     ? "translate-x-[300%]"
//     : translate === 400
//     ? "translate-x-[400%]"
//     : translate === -100
//     ? "translate-x-[-100%]"
//     : translate === -200
//     ? "translate-x-[-200%]"
//     : translate === -300
//     ? "translate-x-[-300%]"
//     : translate === -400
//     ? "translate-x-[-400%]"
//     : "";
// }
